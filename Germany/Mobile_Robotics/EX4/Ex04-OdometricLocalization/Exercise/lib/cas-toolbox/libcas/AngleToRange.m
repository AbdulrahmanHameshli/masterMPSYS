% ar = AngleToRange(a, min);%% Wraps angle a into the interval [min..min+2*pi[. It is% identical to procedure MathE.NormAngle of fw, v.1.0, 24.2.99.% If a is Inf, Inf is returned.% v.1.0, 16.2.00, 8.5.00 koa, ms. Tested.function ar = AngleToRange(a, min);% input argument validity tests% - no testsif a == Inf,  ar = Inf;else  while a >= (min + 2*pi), a = a - 2*pi; end;  while a < min, a = a + 2*pi; end;  ar = a;end;
% h = drawrotellipse(xo, yo, a, b, angle);%% Draws an ellipse with half axis a and b (a>=b), center xo, yo% and inclination angle [rad]. Returns a handle to the graphics% objects for customized graphic styling.% koa, 8.97, 5.1.99, 15.1.99, 9.11.02% v.1.1, 05.01.03, koa: resolution reduced, renamingsfunction h = drawrotellipse(xo, yo, rx, ry, angle);nP = 50;                       % point density or resolutionivec = 0:2*pi/nP:2*pi;         % index vectorp(1,:) = rx*cos(ivec);         % 2 x n matrix whichp(2,:) = ry*sin(ivec);         % hold ellipse pointsR  = [cos(angle) -sin(angle); sin(angle) cos(angle)];T  = [xo; yo]*ones(1,length(ivec));p = R*p + T;				  				 % rotation angle radian and															 % translate to center xo, yoh = plot(p(1,:), p(2,:));